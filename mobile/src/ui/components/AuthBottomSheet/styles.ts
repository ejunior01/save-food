import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 24,
    paddingTop: 8,
  },
  heading: {
    letterSpacing: -0.3,
    marginBottom: 4,
  },
  form: {
    gap: 16,
    marginTop: 20,
  },
});
